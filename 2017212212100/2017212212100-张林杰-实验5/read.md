说明
1. 首先安装相关依赖   安装相关依赖命令 npm i 
2. 当前项目根目录下运行命令  
    1. node index.js -n wang -- 输出wang的赚的全部钱
    2. node index.js -l -- 输出全部学生赚的全部钱列表
    3. node index.js -n wang -l -- 输出wang的列表
    4. node index.js -a -- 输出统计结果